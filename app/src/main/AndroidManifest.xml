<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.unitins.sigam2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.FormActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controllers.CursoActivity" />
        <activity android:name=".controllers.MateriasActivity" />
        <activity android:name=".controllers.PeriodoActivity" />
        <activity android:name=".controllers.AvaliacaoActivity" />
        <!--
        <activity android:name=".controllers.FormActivity"></activity>
        -->
    </application>

</manifest>